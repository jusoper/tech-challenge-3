# Copy to `.env` and adjust locally. Do not commit real secrets.
SEED=42
MODEL_KIND=heuristic
# Alternatives after training: sklearn | onnx
ARTIFACTS_DIR=models/artifacts
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO
